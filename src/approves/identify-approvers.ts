/*
Function that returns required approvers based on file-changes.
Probably logic from getReviewersBasedOnRule should be reused.

But getReviewersBasedOnRule function returns only some specific random users from each group.
And we need full object from our config.
 */

export function identifyApprovers({}: {}) {

}